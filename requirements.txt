asgiref==3.12.1
Django==6.0.7
django-unfold==0.104.1
Pillow==12.1.1
sqlparse==0.5.5
tzdata==2026.3

# Banco de dados MySQL
mysqlclient==2.2.8

# Leitura do .env (segredos fora do repositório)
python-dotenv==1.2.2

# Serve os arquivos estáticos em produção, com compressão e cache
whitenoise==6.12.0

# Servidor WSGI de produção (Linux). No Windows use waitress no lugar.
gunicorn==23.0.0
